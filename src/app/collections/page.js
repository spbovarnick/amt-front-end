import { Suspense } from "react";

const CollectionsPage = ({}) => {

  return(
    <>
      <Suspense fallback={null}>
        Collections
      </Suspense>
    </>
  );
};

export default CollectionsPage;